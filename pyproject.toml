[project]
name = "fix_unique_id_drift"
version = "0.1.0"
description = ""
authors = [
    {name = "Peter DeVita",email = "mewtwo2643@yahoo.com"}
]
requires-python = ">=3.10"
dependencies = [
]

[project.scripts]
fix_unique_id_drift = "fix_unique_id_drift:main"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
